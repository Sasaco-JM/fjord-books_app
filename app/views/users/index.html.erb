<% provide(:title, 'All users') %>
<h1>All users</h1>
 
<% if @users.present? %>
  <ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.user_name, user %>
    </li>
  <% end %>
<% end %>
</ul>

<%= paginate @users %>
