<h1><%= User.model_name.human %></h1>
 
<% if @users.present? %>
  <ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.user_name, user %>
    </li>
  <% end %>
<% end %>
</ul>

<%= paginate @users %>
